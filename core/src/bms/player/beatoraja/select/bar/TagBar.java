package bms.player.beatoraja.select.bar;

import bms.player.beatoraja.song.SongData;
import bms.player.beatoraja.select.MusicSelector;

public class TagBar extends HashBar {
    public TagBar(MusicSelector selector, String title, SongData[] elements) {
        super(selector, title, elements);
    }
}
